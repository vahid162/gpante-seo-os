# Development dependencies for schema contract tests.
#
# Do not uncomment or pin package versions here until they have been
# installed and tested with `python -m unittest tests/test_schema_contracts.py`
# in an environment with package-index access.
#
# Required packages:
# - PyYAML
# - jsonschema
# - referencing
#
# Current execution environment blocker:
# pip and apt package downloads return HTTP 403, so exact tested pins cannot
# be recorded honestly in this PR from this environment.
