# Strategy Approval Workflow

Strategies translate confirmed Findings into recommended direction. A strategy does not authorize production implementation.

Approval requires documented Findings, alternatives, risk, expected outcome, affected URL classes, validation expectations, rollback requirements, and owner approval where repository-wide direction is needed. Approved direction should be recorded in `../decisions/` when it affects cross-run governance or implementation.
