# Finding Triage Workflow

Findings are owned by the producing Run. Triage classifies each Finding as draft, confirmed, deferred, rejected, resolved, or superseded using `status-definitions.md`.

Triage must evaluate evidence strength, affected URL/entity class, severity, confidence, business impact, implementation risk, validation requirements, rollback feasibility, and whether a repository-wide Decision is needed.
